
class Solution:
    def findBall(self, grid: List[List[int]]) -> List[int]:
        rows, cols = len(grid),len(grid[0])
        balls = [0 for i in range(cols)]
               
        for ball in range(0 to cols) :
            for loc in range(0 to rows) :
                if balls[loc] != -1 :
                    if grid[ball][loc] == 1 and grid[ball][loc+1] == 1 :
                        ball
                
        
        print(rows, cols)
        return None

    def chkBall(self,)
    
    